#include <iostream>

int main()
{
    // This program shows a for loop, which is useful when you know how many times you want the loop to run

    for (int count = 1; count <= 5; count++)
    {
        std::cout << "Count: " << count << "\n";
    }

    return 0;
}