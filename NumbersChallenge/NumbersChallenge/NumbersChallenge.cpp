#include <iostream>

int main()
{
    // This introduces the float data type which is used to store decimal numbers also shows 72.5* farenheit display in console
    float temperature = 72.5f;

    std::cout << "The temperature is: " << temperature << "\n";

    return 0;
}